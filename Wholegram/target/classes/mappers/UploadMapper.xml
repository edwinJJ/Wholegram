<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.nigne.wholegram.mapper.UploadMapper">
	
	<insert id="Boardup" >
		insert into Board(user_id,content,media,place,media_type,tag) values(#{user_id},#{content},#{media},#{place},#{media_type},#{tag})
	</insert>
</mapper>